Resumen de trabajo — 16 Ene 2026 → 19 Ene 2026
===============================================

Resumen ejecutivo
-----------------
- Periodo: **16/01/2026 (viernes)** → **19/01/2026 (hoy)**
- **Horas estimadas**: 25 h (ver desglose)
- Estado general: **Completado** — fixes en notificaciones (in-app + email), UI/UX (loading + toasts), reglas de estados (normalización DONE → COMPLETED), correcciones de bloqueo por predecesoras, pruebas y migración aplicada en producción.

Logros y mejoras (resumen)
--------------------------
- Correcciones de notificaciones:
  - Self-assignment ahora genera notificación correctamente.
  - Clientes y usuarios internos reciben notificaciones y correos cuando corresponde.
  - Verificación SMTP y ajuste de configuración `SystemSettings.notify_task_assigned`.

- UX & frontend:
  - Añadido indicador de carga al crear tarea (evita sensación de latencia por envío SMTP).
  - Campana de notificaciones con auto-refresh (poll cada 30s) y *toast* visual cuando llega nueva notificación.
  - Botón "Marcar todo como leído" siempre visible y estado habilitado/deshabilitado sincronizado.

- Lógica de tareas y reglas de estado:
  - Bug: agregar predecesoras en edición no bloqueaba el cambio de estado hasta re-guardar → **fix** y test añadido.
  - Normalización de estados: canonical `COMPLETED`. Legacy `DONE` aceptado y convertido a `COMPLETED` en API y modelo.

- Migraciones y DB:
  - Script idempotente para convertir `DONE` → `COMPLETED` creado y probado localmente (`scripts/normalize_statuses.py`).
  - Alembic migration añadida y aplicada: `migrations/versions/c3f1d2a9b8e7_normalize_done_to_completed.py`.
  - En entorno local/dev: conteo final `DONE = 0`, `COMPLETED = 10`.
  - En producción: `flask db upgrade` ejecutado y revision actual `c3f1d2a9b8e7 (head)`.

- Tests:
  - Añadidos/actualizados tests para: status normalization, predecessor blocking, vistas Kanban/Gantt y cobertura general relacionada.

Desglose estimado de horas
--------------------------
- Debug notificaciones y SMTP: 4 h
- Implementación UI (loading, toasts, bell polling): 4 h
- Mark-all & JS syncing: 2 h
- Predecessor blocking bug + tests: 3 h
- Normalización de estados (modelo, API, templates, tests): 6 h
- Scripts/migración + aplicación en local/prod: 3 h
- Verificaciones, tests y PR/documentación: 3 h
Total: ~25 h

Archivos creados / modificados (destacados)
-------------------------------------------
- Creaciones:
  - `migrations/versions/c3f1d2a9b8e7_normalize_done_to_completed.py`
  - `tests/test_views_status_normalization.py`
  - `scripts/normalize_statuses.py`
  - (actualización) `README_MIGRATIONS.md`

- Cambios importantes:
  - `app/models.py` (Task.normalize_status / set_status)
  - `app/main/routes.py` (mover asignación/validación de predecesoras, Kanban/Gantt inspección)
  - `app/services/notifications.py` (logs + send_email logic)
  - `app/templates/components/_navbar.html` (badge + toast + mark-all)
  - `app/templates/kanban.html`, `gantt.html` (usar `COMPLETED` canonical)
  - Tests bajo `tests/` actualizados para usar `COMPLETED`

Comandos de verificación (útiles para el email)
-----------------------------------------------
- Comprobar revisión aplicada en DB (prod):
  ```bash
  source venv/bin/activate
  flask db current
  # debe mostrar: c3f1d2a9b8e7 (head)
  ```

- Contar tareas por estado (psql):
  ```bash
  psql $DATABASE_URL -c "SELECT count(*) FROM tasks WHERE status='DONE';"
  psql $DATABASE_URL -c "SELECT count(*) FROM tasks WHERE status='COMPLETED';"
  ```

- Quick smoke tests:
  ```bash
  pytest tests/test_views_status_normalization.py -q
  curl -fS http://localhost/health
  ```

Notas operacionales y recomendaciones
-------------------------------------
- Backup antes de aplicar migración en producción (snapshots o pg_dump).
- Ejecutar migración en un único lugar (job o admin node), luego reiniciar workers web y celery/queues.
- Validar Kanban/Gantt manualmente y revisar logs 10–15 minutos post-migration.

Siguiente paso propuesto
------------------------
- Ejecutar suite completa en CI y abrir PR con checklist + nota de release (indico yo el PR si quieres).

Contacto
--------
- Si quieres que prepare el texto del email (listo para pegar) o que abra el PR con los cambios y notas, dime y lo hago.

---
*Generado automáticamente: resumen de trabajo y evidencias para enviar por email.*
